import styled from 'styled-components';
import '../style/var';

export const View = styled.i`
    &.mul-icon {
        .mul-icon__image {
            display: inline-block;
            width: 1em;
            height: 1em;
            object-fit: contain;
        }
    }
`;
